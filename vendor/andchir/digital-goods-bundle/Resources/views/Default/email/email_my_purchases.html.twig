<!DOCTYPE html>
<html>

<head>
    <style type="text/css">
        body { background-color: #fff; }
        p { margin: 0 0 12px 0; }
        td { vertical-align: top; }
        table.table { width: 100%; border: 1px solid #BCBCBC; border-collapse: collapse; }
        table.table th,
        table.table td { padding: 10px; border: 1px solid #BCBCBC; }
        .text-right { text-align: right; }
    </style>
</head>

<body>

<p><b>{{ app_name }}</b></p>

<div style="padding:15px 0; margin:15px 0; border-top:3px solid #BCBCBC; border-bottom:3px solid #BCBCBC;">

    <p>{{ 'Download your purchases by this link' | trans({}, 'e-store') }}:</p>
    <p>
        <a href="{{ downloadUrl }}" target="_blank">{{ downloadUrl }}</a>
    </p>

    <br><br>
    <p>
        <a href="{{ unsubscribeUrl }}" target="_blank" style="font-size:11px; color:#9d9d9d;">
            Unsubscribe
        </a>
    </p>

</div>

<a href="{{ url('homepage') }}" target="_blank">
    {{ url('homepage') }}
</a>

<br><br>

</body>
</html>